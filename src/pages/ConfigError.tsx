export default function ConfigError({ error }: { error: Error }) { return <main className="grid min-h-screen place-items-center p-6"><div className="max-w-lg"><h1 className="text-2xl font-semibold">Konfigurasi belum lengkap</h1><p className="mt-2 text-muted-foreground">Set `VITE_SUPABASE_URL` dan `VITE_SUPABASE_ANON_KEY` di `.env`.</p>{import.meta.env.DEV && <pre className="mt-4 whitespace-pre-wrap rounded bg-muted p-3 text-sm">{error.message}</pre>}</div></main>; }
